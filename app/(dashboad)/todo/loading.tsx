import Logo from '@/components/Logo';

export default function TodoLoading() {
  return (
    <div className="w-full h-full fixed right-0 bottom-0 z-50 flex items-center justify-center bg-white">
      <Logo />
    </div>
  );
}
